import React from 'react';
import ReactDOM from 'react-dom';

export const createFloatingIcon = (x: number, y: number, selectedText: string) => {
  // Create and render a floating icon component
};

export const createResultsModal = (selectedText: string) => {
  // Create and render a results modal component
};
 
